package com.company;

public class Main
{

    public static void main(String[] args)
    {
        new ReverserWindow(); // does reverse and GUI stuff

        // below code is for new comparison of playlists that are similar to find the unique songs
        /*
        System.out.print("Enter first playlist filepath: ");
        Scanner input = new Scanner(System.in);
        String filePath1 = input.nextLine();
        System.out.print("Enter a second playlist filePath: ");
        String filePath2 = input.nextLine();

        input.close();

        new ComparePlaylists(filePath1, filePath2);
        */
    }
}
